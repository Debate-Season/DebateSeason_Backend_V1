# 배너에서 배포판 패치 레벨을 제거한다.
# 기본값(yes)이면 SSH-2.0-OpenSSH_8.9p1 Ubuntu-3ubuntu0.16 처럼
# Ubuntu 패키지 버전까지 노출돼 미적용 보안패치를 특정할 단서가 된다.
# no 로 두면 SSH-2.0-OpenSSH_8.9p1 까지만 나간다.
# (OpenSSH 는 프로토콜상 메이저 버전 문자열 자체는 숨길 수 없다.)
DebianBanner no
