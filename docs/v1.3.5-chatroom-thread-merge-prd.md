# v1.3.5 PRD — 채팅방 스레드 통합 (이슈당 단일 채팅방)

- 작성일: 2026-07-24
- 대상: `domain/chat`, `domain/chatroom`, `config/WebSocketConfig`, `WebSecurityConfig`(경로), DB(`chat_room`, `chat`)
- 상태: **Phase 1·2·3a·3b 프로덕션 라이브 (2026-07-24)** — 남은 것은 §11 `/api/v2/room` 신 클라이언트 엔드포인트 (내일 착수 예정)
- 선행: v1.3.1~v1.3.4 배포 완료 전제 (Role 모델, `created_by`/`status` 컬럼, ADMIN 채팅방 생성 잠금)

> **버전 표기 주의**: `fix(v1.3.5)` 커밋(#186, optional-auth 401 정합성)이 이미 같은 번호로 배포됐다.
> 이 문서는 v1.3.4 PRD 가 **"v1.3.5 이후 후보"로 유보**했던 스레드 구조 재편을 다룬다(그 PRD §7).
> 커밋 태그 충돌이 신경 쓰이면 이 배포를 **v1.3.6** 으로 올릴 수 있다 — 파일명만 바꾸면 된다.
> (본문 로직은 번호와 무관.)

> **v1.3.4 PRD 와의 관계는 §4 참조.** 이 문서는 v1.3.4 PRD 가 그린 DebateTopic/토론위키 전체 그림을
> **다시 설계하지 않는다.** 그중 "이슈당 여러 채팅방을 하나로 합치고 기존 방을 스레드로 강등"하는
> **최소 통합 단계만** 떼어내 먼저 배포하는 것이 v1.3.5 다.

---

## 0. 한눈에

**지금**: 이슈 하나에 채팅방(`chat_room`)이 여러 개(최대 3개) 매달려 있고, 방마다 채팅 내역이 갈라져 있다.
모바일은 이슈 안에서 방을 여러 개 본다.

**바꾼 뒤**: 이슈당 **컨테이너 채팅방 1개**만 남는다. 기존 방들은 **스레드**로 강등되고, 흩어져 있던
채팅이 컨테이너의 **하나의 전체 내역**으로 합쳐진다.

- **모바일**: 이슈당 채팅방 하나. 들어가면 전체 채팅 스트림(스레드 구분 없이).
- **웹**: 같은 채팅방에서 `[전체][주제A][주제B]` 탭으로 스레드별 필터 조회.
- **찬반·팀점수·MVP·알림·멤버십**: 스레드(기존 방)에 **그대로** 남는다 → 데이터 충돌·손실 없음.
- **메시지만** 컨테이너로 통합(`chat.chat_room_id → 컨테이너`, `chat.thread_id → 기존 방`).

**규모(2026-07-23 prod 실측)**: 이슈 17, 채팅방 26, **여러 방을 가진 이슈 8개**, 이슈당 최대 3방,
메시지 386건. → 마이그레이션은 순간. 관건은 데이터가 아니라 **실시간 주소·클라이언트 계약**(§6).

---

## 0.5 진행 현황 (2026-07-24)

**핵심 통합은 프로덕션에 라이브다.** 모바일은 이슈당 채팅방 1개로 전체 내역이 합쳐져 보이고,
웹은 스레드 태그로 탭 필터가 가능하며, 구 앱은 안 깨진다.

| 단계 | 내용 | 상태 |
|---|---|---|
| Phase 1 (#188) | Expand 스키마(`chat.thread_id`, `chat_room.room_type`/`container_room_id`) + `threadId` 배선 | ✅ 배포 |
| Phase 3a (#189) | 쓰기·읽기 라우팅 (옛 방=THREAD → `chat_room_id`=컨테이너, `thread_id`=옛 방) | ✅ 배포 |
| Phase 3b (#190) | 이관 전/후 모두 안전한 읽기 계층(집계) | ✅ 배포 |
| **Phase 2 데이터 이관** | `docs/sql/v1.3.5-expand.sql` 적용 + `docs/sql/v1.3.5-migrate.sql` 실행 | ✅ 완료·검증 |
| **Phase 3b-endpoint** | `/api/v2/room` 신 클라이언트 진입점 + 방 목록 컨테이너 노출 | ⬜ **남음 (§11)** |

**이관 결과(prod 실측)**: 386 메시지 → 17 컨테이너로 통합, 26 기존 방을 스레드로 강등·태깅.
검증 예: 이슈6 컨테이너 = 195건 = 스레드57(113) + 스레드58(82).

### ⚠ 배포 순서 교훈 (다음 유사 작업 때 반드시 참고)
**읽기 라우팅·안전 집계(3a·3b)를 데이터 이관보다 먼저 배포해야 한다.**
최초 이관을 3b 배포 전에 실행했다가, 메시지가 컨테이너로 옮겨지며 **방 상세의 팀점수·MVP,
홈 Top5 랭킹, 최근시각이 빈값이 되는 읽기 회귀**를 발견했다. → 이관을 **롤백**하고 3b를 배포한 뒤
**재이관**했다. 트랜잭션 dry-run→commit(InnoDB) 이라 롤백이 가능했다.

- 집계 read 는 이관 전/후 모두 동작하도록 `(thread_id=:id OR (thread_id IS NULL AND chat_room_id=:id))`
  / `COALESCE(thread_id, chat_room_id)` 로 통일했다(Phase 3b). 그래서 코드를 먼저 배포해도 무회귀.
- **롤백 시 컨테이너 행이 DELETE 되므로 재이관 때 컨테이너 id 가 새로 발급된다**(auto-increment).
  운영 중 컨테이너 id 를 어디 하드코딩하지 말 것.
- 백업: 서버 `~/backups/pre_migrate*_v135_*.sql` (+ 자동 일일 백업).

---

## 1. 목표 / 비목표

### 목표
1. 이슈당 컨테이너 채팅방 1개를 두고, 기존 다중 방을 그 아래 스레드로 강등한다.
2. 기존 채팅 내역을 컨테이너의 단일 전체 스트림으로 통합한다(메시지 유실 0).
3. 모바일: 이슈당 채팅방 하나 + 전체 스트림. 웹: 스레드 탭 필터.
4. 구 앱을 깨지 않는다(기존 방=스레드가 살아 있어 구 앱 투표·조회 경로 유지).

### 비목표 (이번 배포에서 안 함)
- **DebateTopic / DebateWiki / 리비전** 신규 엔티티 (v1.3.4 PRD §3.1·3.3) — §4 로 연기.
- **유저의 스레드(주제) 생성 기능** — 이번엔 기존 방을 스레드로 강등만. 신규 스레드 생성 API 없음.
- **찬반의 단위 변경** — 스레드(기존 방) 단위 그대로. `user_topic_opinion` 신설 안 함.
- Event(사건) / 미디어 클러스터링 / Presence — v1.3.4 PRD 소관.

---

## 2. 현황 (As-Is)

데이터·권한 현황 상세는 **v1.3.4 PRD §2** 를 그대로 따른다. 이 문서에 필요한 핵심만:

```
issue (1) ──< chat_room (N)   chat_room.issue_id       ← 채팅방 = 토론 주제 (한 엔티티가 안건=방=컨테이너 3역)
chat_room (1) ──< chat (N)    chat.chat_room_id        ← 메시지는 방에 소속
chat_room (1) ──< user_chat_room (N)                   ← 찬반(opinion)·관심(interest) = 방 단위
chat_room (1) ──< chatroom_notification_setting        ← 복합PK (user_id, chat_room_id)
```

- `chat_room_id` 하나가 **① STOMP 주소 ② 메시지 FK ③ 찬반·멤버십 키 ④ 알림 키** 를 동시에 짊어진다.
- `thread`/`topic`/`debate` 개념은 **코드에 아직 없다**(스키마·엔티티·API 전무).

---

## 3. To-Be 모델

### 3.1 컨테이너 + 스레드

```
issue ──< chat_room
            ├─ CONTAINER  (이슈당 1개, 신규 생성)   ← 모바일이 보는 "그 이슈의 채팅방", 메시지 버킷
            ├─ THREAD "전체" (이슈당 1개, 신규 생성) ← 모바일/무주제 글의 기본 착지
            └─ THREAD 주제A·B… (기존 chat_room 을 강등)  ← 투표·팀점수·MVP·멤버십·알림 그대로 보존
```

- **CONTAINER 는 새 `chat_room` 행**이다. 새 테이블을 만들지 않고 기존 방-기반 기계(STOMP·히스토리·FK)를
  그대로 재사용하기 위함. 컨테이너 자체는 투표를 갖지 않는다(메시지 버킷 + 이슈 진입점).
- **THREAD 는 기존 `chat_room` 행**을 그대로 둔 것이다. `type=THREAD` 로만 표시. 여기 걸린
  `user_chat_room`(찬반)·`chatroom_notification_setting`·집계 쿼리는 **손대지 않는다**.
- 방 구분용 컬럼을 `chat_room` 에 추가한다(§5.1). 조회 시:
  - 이슈의 컨테이너: `type=CONTAINER AND issue_id=?`
  - 이슈의 스레드 목록: `type=THREAD AND issue_id=?`

### 3.2 chat.thread_id (신규 컬럼)

```
ChatEntity + thread_id (nullable, FK → chat_room.chat_room_id = 스레드)
   chat.chat_room_id → CONTAINER   ← 마이그레이션 후 모든 메시지가 컨테이너 소속
   chat.thread_id    → THREAD      ← 원래 어느 방(주제)에서 온 메시지인지
```

- 마이그레이션 시 기존 메시지: `thread_id = 원래 chat_room_id`, `chat_room_id = 그 이슈의 컨테이너`.
- 모바일 신규 글: `thread_id = 전체 스레드`(기본). 웹 신규 글: `thread_id = 선택 탭`.

### 3.3 찬반·멤버십·알림 — 스레드에 그대로 (충돌 없음)

- `user_chat_room`(opinion AGREE/DISAGREE, interest), `chatroom_notification_setting`,
  팀점수/MVP/하이라이트 집계(`ChatRoomRepository`) — **전부 스레드(기존 방) 키 그대로 유지**.
  → N→1 붕괴에 따른 dedup/PK 충돌이 **아예 발생하지 않는다.**
- 컨테이너의 "멤버십"은 저장하지 않고 **조회 시 파생**한다: 이슈의 어느 스레드든 가입/투표했으면
  컨테이너 참여자. 알림도 스레드 설정의 **합집합**(하나라도 ON → 이슈 알림 ON).

### 3.4 실시간 (STOMP) — 목적지 패턴 유지, payload 에 threadId 추가

현재: 발행 `@MessageMapping("/chat.room.{roomId}")`, 구독 `@SendTo("/topic/room{roomId}")`
(`WebSocketControllerV1.java:37-38`). `{roomId}` 가 곧 `chat_room_id`.

**변경 최소안(권장)**:
- 브로드캐스트는 **컨테이너 주소 하나** `/topic/room{containerId}` 로 유지.
- **응답 payload 에 `threadId` 필드만 추가**한다.
- 모바일: 컨테이너 구독 → 전부 표시. 웹: 같은 컨테이너 구독 → `threadId` 로 탭 필터(클라이언트 사이드).
- → STOMP 목적지 문법을 안 바꾼다. `@SendTo` 값도 그대로. **재구독/주소 체계 변경 없음.**

> 대안(스레드별 목적지 `/topic/room{c}.thread{t}`)은 구독 granularity 를 서버가 갖지만
> 컨트롤러·클라이언트 변경이 커진다. 웹 탭 전환이 클라이언트 필터로 충분하므로 **최소안 채택.**

### 3.5 조회 모드

| 모드 | 쿼리 | 용도 |
|---|---|---|
| 전체 스트림 | `WHERE chat_room_id = :containerId` | 모바일 기본 화면 / 웹 [전체] 탭 |
| 스레드 필터 | `WHERE chat_room_id = :containerId AND thread_id = :threadId` | 웹 주제 탭 |

- 전체 스트림은 **기존 커서 쿼리 구조 그대로**(`findByRoomIdAndCursor`) — roomId 에 컨테이너 id 를 넣으면 됨.
- 스레드 필터는 `thread_id` 조건이 붙은 쿼리를 신설. **커서는 모드별 독립**(전체 커서를 스레드 뷰에 재사용 금지).
- **인덱스**: `chat(chat_room_id, chat_id)`(전체 스트림 커서) + `chat(thread_id, chat_id)`(스레드 필터) 추가.
  `chat` 은 가장 빨리 커지는 테이블 — 인덱스 없이 열면 즉시 느려진다(v1.3.4 PRD §6.5 지적).
- **날짜 조회 주의**: `findByRoomIdAndCursorAndDate` 가 `DATE(time_stamp)=` 로 인덱스를 못 탄다.
  스레드 필터에 같은 패턴 복사 금지 — 범위 조건(`time_stamp >= :start AND < :end`)으로.

### 3.6 API 계약 변화

| 엔드포인트 | 변화 | 호환 |
|---|---|---|
| 채팅 히스토리 `GET /api/v1/chat/rooms/{roomId}/messages` | roomId=컨테이너 시 전체 스트림. `?threadId=` 옵션 추가로 스레드 필터. 응답 메시지에 `threadId` 필드 추가 | 응답 필드 추가 = 안전. 구 앱은 무시 |
| STOMP 브로드캐스트 payload | `threadId` 필드 추가 | 안전 |
| 메시지 전송 `ChatMessageRequest` | `threadId`(optional) 추가 | 구 앱 미전송 → 기본 스레드 착지 |
| 방 목록/입장 (`findVotedChatRoom`, `GET /api/v1/room`) | **신규 클라이언트에 컨테이너를 노출**해야 함 | §6 — 신 경로/헤더로 분기 |

---

## 4. v1.3.4 PRD 와의 관계 (반드시 읽을 것)

v1.3.4 PRD(§3.1·3.2·3.7)는 스레드를 **DebateTopic 신규 엔티티**로, 찬반을 **`user_topic_opinion`** 으로
설계했다. v1.3.5 는 그 그림의 **경량 선행 단계**로, 다음을 **의도적으로 다르게** 한다:

| 항목 | v1.3.4 PRD (전체 그림) | v1.3.5 (이번) |
|---|---|---|
| 스레드 실체 | 신규 `debate_topic` 테이블, 유저 생성 | **기존 `chat_room` 을 강등** (신규 테이블 없음) |
| chat 의 스레드 FK | `topic_id → debate_topic` | `thread_id → chat_room`(스레드) |
| 찬반 | `user_topic_opinion` 로 이관 | **`user_chat_room` 그대로**(스레드=방이므로 이미 주제 단위) |
| 위키/리비전 | 신설 | 안 함 |
| 유저 스레드 생성 | 있음 | 없음(강등만) |

**왜 경량으로?** 지금 필요한 것은 "흩어진 방을 하나로 합쳐 모바일에 단일 채팅방을 주는 것"뿐이다.
기존 방들은 이미 title/content/created_by/status/투표를 가진 **사실상의 경량 주제**이므로,
새 테이블 없이 그대로 스레드로 쓰는 것이 최소 변경이다.

**미래 경로 (DebateTopic 로 승격 시)**: 강등된 방(스레드)들이 곧 seed DebateTopic 이 된다.
그때 `thread_id`(→chat_room) 를 `topic_id`(→debate_topic)로 옮기거나, `debate_topic` 를
강등된 방에 1:1 로 얹으면 된다. **`thread_id` 컬럼명을 특정 테이블에 종속되지 않게 유지**하면 전환 비용이 준다.

> **결정 포인트(§10-1)**: 이 divergence 를 감수할지, 아니면 처음부터 `debate_topic` 을 만들지.
> 데이터 규모(스레드 26건)가 작아 나중 승격도 저비용이므로 **경량 선행을 권장.**

---

## 5. 마이그레이션 (Expand / Migrate / Contract)

원칙은 v1.3.4 PRD §6.2 를 그대로 따른다. **`chat_room` PK 는 절대 건드리지 않는다.**

### 5.1 Expand — 스키마 추가 (하위호환, 무해)

```sql
-- chat_room: 컨테이너/스레드 구분 + 컨테이너 역참조
ALTER TABLE chat_room ADD COLUMN room_type VARCHAR(16) NULL;      -- 'CONTAINER' | 'THREAD' | NULL(레거시=THREAD 취급)
ALTER TABLE chat_room ADD COLUMN container_room_id BIGINT NULL;   -- THREAD → 소속 컨테이너 (CONTAINER 는 NULL)

-- chat: 스레드 참조 + 커서 인덱스
ALTER TABLE chat ADD COLUMN thread_id BIGINT NULL;               -- FK → chat_room(스레드). NULL=미분류
ALTER TABLE chat ADD INDEX idx_chat_room_cursor (chat_room_id, chat_id);
ALTER TABLE chat ADD INDEX idx_chat_thread_cursor (thread_id, chat_id);
```

- 386행 / MariaDB 10.6 → `ADD COLUMN`·`ADD INDEX` 모두 순간(v1.3.4 PRD §6.5).
- 이 단계는 구/신 클라이언트 **모두 무영향**. 코드는 `thread_id` 있으면 저장, 없으면 기존대로.

### 5.2 Migrate — 데이터 이관 (프로덕션 쓰기 — **별도 승인 필수**)

이슈별로(여러 방을 가진 8개 이슈가 핵심, 단일 방 이슈도 균일 처리):

```
FOR EACH issue:
  1. 컨테이너 생성: chat_room(room_type=CONTAINER, issue_id, title=이슈제목, created_by=NULL)
  2. "전체" 스레드 생성: chat_room(room_type=THREAD, issue_id, container_room_id=컨테이너, title='전체')
  3. 기존 방들 강등: UPDATE chat_room SET room_type='THREAD', container_room_id=컨테이너
                     WHERE issue_id=? AND room_type IS NULL
  4. 메시지 재지정: UPDATE chat SET thread_id = chat_room_id, chat_room_id = 컨테이너
                   WHERE chat_room_id IN (그 이슈의 강등된 방들)
```

- **백업 먼저**: `chat`, `chat_room` 스냅샷(mysqldump 또는 `CREATE TABLE _bak AS SELECT`).
- **dry-run**: 트랜잭션 안에서 실행 후 건수 대조 → 문제없으면 커밋. 롤백 스크립트 동반.
- **검증**: 이관 전/후 `COUNT(chat)` 동일, 이슈별 메시지 합 보존, 컨테이너당 스레드 수 = 강등방+1(전체).
- **찬반/멤버십/알림은 건드리지 않는다** — 스레드에 그대로 남으므로 이관 대상 아님(충돌 원천 제거).

### 5.3 Contract — 레거시 제거 (스토어 반영 + force_update 이후)

- 신규 클라이언트 확산 확인 후, 구 방-목록 경로/구 조회 분기 정리.
- 자세한 시점 규칙은 v1.3.4 PRD §6.1-1(배포 시차) 그대로.

---

## 6. 클라이언트 호환성

v1.3.4 PRD §6.1~§6.4 의 프레임(Expand/Migrate/Contract, 강제 업데이트의 한계, 실측)을 그대로 적용한다.
**v1.3.5 는 "방 재활용 + 스레드별 투표 유지" 덕분에 구 앱 파손 위험이 v1.3.4 안보다 낮다:**

- 구 앱이 실제로 쓰는 쓰기 API 는 `POST /api/v1/room/vote` 하나(§6.3-1 실측). 이 방들이 스레드로
  **그대로 살아 있으므로** 구 앱 투표·`GET /api/v1/room?chatroom-id=<옛방>` 조회는 **무변경으로 동작**한다.
- 즉 v1.3.4 PRD §6.4 의 "대표 주제 shim" 이 **거의 필요 없다** — 구 앱은 자기가 알던 방(=스레드)을 계속 쓴다.

남는 유일한 호환 과제: **방 목록/진입에서 신규 클라이언트에 컨테이너를 노출**하는 것.
- 구 앱: 지금처럼 스레드(옛 방)들이 목록에 뜨거나(무변경), 또는 그대로 유지.
- 신 앱/웹: 컨테이너 1개를 본다.
- 분기 수단: `/api/v2/room` 신규 엔드포인트(레포에 `/api/v2/users/login` 전례) **또는**
  `X-Client-Version` 헤더(v1.3.4 PRD §6.1-2). → **§10-2 결정**.

---

## 7. 영향 표면 인벤토리 (file:line)

코드 스윕으로 확인한 `chat_room_id` 결합 지점. 이번 변경이 건드리는 곳:

**실시간**
- `config/WebSocketConfig.java:54-58` — 브로커 prefix(방 id 없음, 무영향)
- `domain/chat/presentation/controller/WebSocketControllerV1.java:37-38,46` — `{roomId}` 목적지·`processChatMessage(roomId,…)`. payload 에 `threadId` 추가

**쓰기**
- `domain/chat/presentation/dto/chat/request/ChatMessageRequest.java:28` — `roomId` 옆에 `threadId`(optional) 추가
- `domain/chat/application/service/ChatServiceV1.java:58-82,99-118` — `chat_room_id=컨테이너, thread_id=thread` 저장
- `domain/chat/event/ChatMessageEventListener.java:22-27` — async 저장 경로도 동일 반영
- `domain/chat/infrastructure/chat/ChatEntity.java:28-30,58-69` — `thread_id` 필드/매퍼 추가

**히스토리**
- `domain/chat/presentation/controller/ChatControllerV1.java:89-112` — `?threadId=` 옵션
- `domain/chat/application/service/ChatServiceV1.java:122-196,200-271` — 스레드 필터 분기
- `domain/chat/infrastructure/chat/ChatJpaRepository.java:17-65` — 5개 쿼리(전체 스트림은 컨테이너로 무회귀, 스레드 필터 쿼리 신설)
- `domain/chat/application/repository/ChatRepository.java:15-26`, `.../ChatRepositoryImpl.java:29-55`

**방 목록/입장 (신규 클라이언트에 컨테이너 노출)**
- `domain/chatroom/controller/ChatRoomControllerV1.java:63-72` — `GET /api/v1/room`(컨테이너/스레드 구분)
- `domain/issue/presentation/controller/IssueControllerV1.java:33,86` — 홈 목록 `findVotedChatRoom`
- `domain/chatroom/service/ChatRoomServiceV1.java:136-330,339-524` — 목록/집계가 컨테이너 기준일지 스레드 기준일지 분리
- `domain/chatroom/infrastructure/entity/manager/ChatRoomManager.java:26-91`, `.../processor/ChatRoomProcessor.java:35-99` — 랭킹 granularity(스레드 유지 권장)

**손대지 않는 곳(스레드에 그대로 남김)**
- `user_chat_room`(찬반·관심) 및 `UserChatRoomRepository` 전 쿼리 — 스레드 키 유지
- `chatroom_notification_setting` 복합PK, `NotificationServiceV1` 푸시 팬아웃 — 스레드 유지(컨테이너 알림은 파생)
- `ChatRoomRepository` 집계(팀점수·MVP·하이라이트) — 스레드 유지
- `chat_reaction`(chat_id FK), `report`(target=chat_id) — 무영향

**테스트(계약 검증 → 갱신 필요)**
- `test/.../integration/chat/ChatWebSocketTest.java`, `test/.../domain/chat/controller/WebSocketControllerV1Test.java` — `/chat.room.{roomId}` ↔ `/topic/room{roomId}` 매핑 + `threadId` payload

---

## 8. 롤아웃 단계 & 체크리스트

| Phase | 내용 | 배포/승인 |
|---|---|---|
| **1. Expand+쓰기** | §5.1 스키마 추가 + 쓰기 경로가 `threadId` 저장(있으면), STOMP payload 에 `threadId`. 하위호환 | 즉시 배포 가능(무해) |
| **2. Migrate** | §5.2 컨테이너/전체스레드 생성 + 기존 방 강등 + 메시지 재지정 | **프로덕션 DB 쓰기 — 별도 승인.** 백업·dry-run·롤백 동반 |
| **3. 읽기/실시간** | 히스토리 스레드 필터, 방 목록의 컨테이너 노출(신 클라이언트), 웹 탭 | 배포. 웹 동시 반영 |
| **4. Contract** | 구 경로 정리 | 스토어 반영 + force_update 이후 |

**Phase 2 착수 전 필수 확인**
- [ ] `chat`, `chat_room` 백업 완료
- [ ] `chat_room` 에 이미 `room_type` 이 채워진 행 없음(재실행 안전성)
- [ ] 이슈별 방 수·메시지 수 사전 스냅샷(이관 후 대조용)
- [ ] 롤백 스크립트 리허설(스테이징 또는 트랜잭션 dry-run)

---

## 9. 리스크

| 리스크 | 영향 | 완화 |
|---|---|---|
| 방 목록이 컨테이너/스레드를 뒤섞어 노출 | 모바일에 방이 이상하게 보임 | `room_type` 필터를 목록·홈·랭킹 전 경로에 일관 적용(§7) |
| 스레드 필터 커서를 전체 스트림과 공유 | 페이지 어긋남 | 모드별 독립 커서(§3.5) |
| `chat` 인덱스 누락 | 스레드 필터·커서 느려짐 | Expand 에서 두 복합 인덱스 동시 추가(§5.1) |
| 마이그레이션 재실행 | 컨테이너 중복 생성 | `room_type IS NULL` 가드 + 멱등 스크립트 |
| STOMP payload 만 바꾸고 히스토리 응답 누락 | 웹 탭이 실시간엔 뜨는데 새로고침하면 사라짐 | payload·히스토리 응답 **양쪽에 `threadId`** 동시 반영 |
| DebateTopic 를 나중에 만들며 `thread_id` 재작업 | 이중 스레드 FK | `thread_id` 컬럼명 테이블 비종속 유지(§4 미래 경로) |

---

## 10. 열린 결정 — 전부 확정됨

**10-1. 스레드 실체** → ✅ **경량(기존 방 강등)** 채택. 신규 테이블 없이 `chat_room` 을 THREAD 로 강등.

**10-2. 신규 클라이언트 컨테이너 노출** → ✅ **(a) `/api/v2/room` 신규 엔드포인트** 채택 (§11 에서 구현).

**10-3. "전체" 의미** → ✅ **`thread_id=NULL` 을 "전체/미분류"로 취급**. 별도 "전체" 스레드 행은 만들지 않는다.
웹 [전체] 탭 = 컨테이너 전체 스트림(필터 없음), 주제 탭 = `thread_id` 필터. 모바일 무주제 글 = `thread_id` NULL.

---

## 11. 남은 작업 — Phase 3b-endpoint (내일 착수 예정, 2026-07-25~)

**핵심 통합은 이미 라이브(§0.5).** 남은 것은 **신 클라이언트가 컨테이너를 발견하는 진입점**뿐이다.
프론트(웹/모바일)가 붙여야 사용자에게 노출되므로 프론트 협업이 필요하다. 백엔드 변경은 **추가만(additive)**
이라 기존/구 앱에 무영향.

### 11.1 `GET /api/v2/room?issue-id=` — 컨테이너 + 스레드 목록
신 클라이언트가 이슈로 진입할 때 "그 이슈의 채팅방(컨테이너) 1개 + 스레드 목록"을 받는다.

- 응답(안):
  ```
  {
    containerRoomId,           // 모바일이 구독/입장할 방
    issueId, issueTitle,
    threads: [ { threadId, title, agreeCount, disagreeCount, myOpinion } ]  // 웹 탭
  }
  ```
- 스레드의 찬반/내 투표는 기존 `user_chat_room`(스레드=옛 방 키)에서 그대로 집계.
- 신규 리포지토리 메서드: 컨테이너 조회 `findFirstByIssueEntity_IdAndRoomType(issueId, CONTAINER)`,
  스레드 목록 `findByContainerRoomId(containerId)` (또는 `issue_id + room_type=THREAD`).
- 신규 컨트롤러 `ChatRoomControllerV2`(`/api/v2/room`). **`WebSecurityConfig.OPTIONAL_AUTH_URLS` 에
  `/api/v2/room` 추가** (v1 처럼 optional-auth: 로그인 시 myOpinion 채움, 비로그인도 조회 가능).

### 11.2 방 목록/홈의 컨테이너 노출 (신 클라이언트용)
- 현재 홈/목록(`findVotedChatRoom`, Top5)은 **스레드(옛 방)를 노출**한다 → 구 앱 무변경으로 정상.
- 신 앱은 이슈당 컨테이너를 봐야 하므로, v2 목록 경로 또는 위 `/api/v2/room` 로 흡수.
- 결정 필요: 홈 랭킹을 스레드(주제) 단위로 유지할지, 컨테이너(이슈) 단위로 바꿀지 — 프론트 UX 에 종속.

### 11.3 실시간(STOMP) — 확인 사항 (코드 변경 없이 동작 중)
- 브로드캐스트는 `/topic/room{roomId}` 유지, payload 에 `threadId` 포함(Phase 1 완료).
- **전환기 실측 주의**: 구 앱은 옛 방(스레드) 토픽으로, 신 앱은 컨테이너 토픽으로 구독 → 두 집단의
  실시간이 분리된다(각자 정상, 상대방 메시지는 새로고침 시 반영). 강제 업데이트 확산 후 자연 해소.
- 반응(reaction) 브로드캐스트(`ChatReactionServiceV1`)도 컨테이너 기준 → 동일한 전환기 분리. 데이터는 정상.

### 11.4 Contract (나중)
- 신 앱 확산 확인 후 구 목록 경로 정리. 시점 규칙은 v1.3.4 PRD §6.1-1.

### 착수 체크리스트
- [ ] `ChatRoomType`/`ChatRoom`(container_room_id) 는 이미 있음 — 재사용
- [ ] `ChatRoomRepository`: 컨테이너/스레드 조회 파생 메서드 2개
- [ ] `ChatRoomControllerV2` + 응답 DTO + 서비스 메서드
- [ ] `WebSecurityConfig.OPTIONAL_AUTH_URLS` 에 `/api/v2/room` 추가
- [ ] 테스트 + 프론트에 계약 공유

---

## 부록 — prod 실측 (2026-07-23, 읽기 전용)

| 항목 | 값 |
|---|---|
| issue | 17 |
| chat_room | 26 |
| **여러 방을 가진 issue** | **8** |
| 이슈당 최대 방 수 | 3 |
| chat | 386 |
| user_chat_room | 453 |
| 메시지 있는 방 | 20 |
